unit load;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, ExtCtrls;

type
  Tfrm_LoadWG = class(TForm)
    Label2: TLabel;
    Label4: TLabel;
    Timer1: TTimer;
    procedure Timer1Timer(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  frm_LoadWG: Tfrm_LoadWG;

implementation

{$R *.dfm}

procedure Tfrm_LoadWG.Timer1Timer(Sender: TObject);
begin
Close;
end;

end.
